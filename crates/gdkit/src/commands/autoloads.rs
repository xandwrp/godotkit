//! `autoloads` (offline): Project::discover → settings().autoloads() → Display or JSON.

use crate::cli::*;
use crate::context::Context;
use crate::render::Exit;

pub fn run(ctx: &Context, args: ProjectArgs) -> gdproject::Result<Exit> {
    todo!()
}
